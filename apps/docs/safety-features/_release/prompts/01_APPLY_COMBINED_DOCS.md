# 01_APPLY_COMBINED_DOCS

```text
너는 문서 overlay 적용을 담당하는 개발자다.

목표:
`safety_features_step14_combined_docs_overlay.zip`을 프로젝트 루트에 적용하고, docs/safety-features 구조가 정상 생성되었는지 확인하라.

절차:
1. 기존 docs/safety-features가 있으면 백업한다.
2. overlay zip을 프로젝트 루트에서 압축 해제한다.
3. INDEX.md, _registry, _quality, _release 폴더 존재 여부를 확인한다.
4. 앱 코드는 수정하지 않는다.

완료 기준:
- docs/safety-features/INDEX.md 존재
- docs/safety-features/_registry/feature_registry.md 존재
- docs/safety-features/_release/specs/final_qa_order.md 존재
```
