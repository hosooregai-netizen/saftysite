# 15_IMPLEMENT_EVIDENCE_LINKING_ENTRY

```text
너는 사진첩과 보고서 evidence linking 진입점을 설계/구현하는 시니어 프론트엔드 엔지니어다.

목표:
PhotoAlbumItem detail에서 보고서 evidence 연결 상태와 CTA를 제공하라.

요구사항:
1. sourceReportKey/sourceSlotKey가 있으면 연결 badge를 표시하라.
2. 연결된 보고서에서 열기 링크를 제공하라.
3. 미연결 사진은 “보고서에 연결” CTA를 표시하라.
4. MVP에서는 연결 modal을 placeholder로 둬도 되지만, payload contract를 문서화하라.
```
