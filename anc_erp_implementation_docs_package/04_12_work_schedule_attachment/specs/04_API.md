# 04. API — 공사일정 첨부/공정표 첨부

## Primary Trace API

```text
GET /api/v1/work-schedule-attachment-forms/{formId}
```

## Common Pattern

```text
GET list
POST create
GET detail
PATCH update
POST validate
POST preview optional
POST export optional
```
