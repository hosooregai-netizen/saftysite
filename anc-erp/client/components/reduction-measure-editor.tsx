export function ReductionMeasureEditor({ text }: { text: string }) {
  return (
    <article className="ops-item">
      <strong>ReductionMeasureEditor</strong>
      <span>{text}</span>
    </article>
  );
}
