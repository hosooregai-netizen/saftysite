# Batch 87: Admin Overview Donut Center Count

- Restored the visible total count inside admin overview donut charts.
- Kept descriptive center labels removed so only the numeric count appears inside the graph.
- No API, schema, or backend changes are required.

