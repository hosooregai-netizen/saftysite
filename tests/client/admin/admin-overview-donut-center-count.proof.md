# Admin Overview Donut Center Count Proof

- Static proof: `npm run lint`
- The change is limited to `features/admin/sections/overview/OverviewVisualCards.tsx`.
- Donut chart labels remain outside in the legend while the center shows only the total number.

