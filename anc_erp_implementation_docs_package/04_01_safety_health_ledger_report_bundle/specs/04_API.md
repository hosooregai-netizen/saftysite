# 04. API — 공사안전보건대장 이행확인 보고서 묶음

## Primary Trace API

```text
GET /api/v1/safety-health-ledger-report-bundles/{bundleId}
```

## Common Pattern

```text
GET list
POST create
GET detail
PATCH update
POST validate
POST preview optional
POST export optional
```
