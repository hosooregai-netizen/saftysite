# 04. API — 안전보건대장 자동화

## Primary Trace API

```text
GET /api/v1/safety-health-ledgers/{ledgerId}
```

## Common Pattern

```text
GET list
POST create
GET detail
PATCH update
POST validate
POST preview optional
POST export optional
```
