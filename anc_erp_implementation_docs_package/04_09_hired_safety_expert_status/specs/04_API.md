# 04. API — 발주자가 고용한 안전보건 전문가 현황

## Primary Trace API

```text
GET /api/v1/hired-safety-expert-forms/{formId}
```

## Common Pattern

```text
GET list
POST create
GET detail
PATCH update
POST validate
POST preview optional
POST export optional
```
