# 04. API — 점검회차/일정 관리

## Primary Trace API

```text
GET /api/v1/projects/{projectId}/inspection-rounds
```

## Common Pattern

```text
GET list
POST create
GET detail
PATCH update
POST validate
POST preview optional
POST export optional
```
