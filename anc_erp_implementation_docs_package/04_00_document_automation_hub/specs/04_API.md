# 04. API — 표준서식 자동화 허브

## Primary Trace API

```text
GET /api/v1/document-bundles/{bundleId}
```

## Common Pattern

```text
GET list
POST create
GET detail
PATCH update
POST validate
POST preview optional
POST export optional
```
